name: E2E Tests (Nightly)

on:
  schedule:
    - cron: '0 2 * * *'  # Every night at 2:00 UTC
  workflow_dispatch:

jobs:
  e2e-full:
    uses: ./.github/workflows/e2e-common.yaml
    with:
      suite: full
